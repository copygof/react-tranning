const ImageSource = {
  // Example for defined image source
}

const IconSource = {
  // Example for defined icon source
  // HAMBURGER: require('./icons/hamburger.png'),
}

export { ImageSource, IconSource }
